Use minitest for tests.
Write unit tests for new code.

Never use webmock or stub methods.
Use dependency injection with inert substitutes configured with return values where needed.
Reference agent_docs/useful_objects.md for dependency injection patterns.
